SetSlippageTolerance_Integration_Concrete_Test
├── when caller not comptroller
│  └── it should revert
└── when caller comptroller
   ├── when slippage tolerance exceeds max
   │  └── it should revert
   └── when slippage tolerance not exceed max
      ├── it should update the new slippage tolerance
      └── it should emit a {SetSlippageTolerance} event

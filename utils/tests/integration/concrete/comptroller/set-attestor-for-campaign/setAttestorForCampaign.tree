SetAttestorForCampaign_Integration_Concrete_Test
├── when caller not admin
│  ├── when caller without attestor manager role
│  │  └── it should revert
│  └── when caller with attestor manager role
│     └── it should succeed
└── when caller admin
   ├── when call reverts
   │  └── it should revert
   └── when call does not revert
      └── it should succeed
